const Layout = ({ children }) => {
    return <div style={{ paddingLeft: "285px" }}>
        <div style={{ margin: "0 43px" }}>
            {children}
        </div>
    </div>
};

export default Layout;